# kafka-local

Local Kafka broker (KRaft mode, no Zookeeper) for running `agent-service` and `audit-service` locally during development.

## Usage

    docker compose up -d

Broker available at `localhost:9092`.
